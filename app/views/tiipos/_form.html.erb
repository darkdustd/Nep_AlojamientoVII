<%= form_with(model: tiipo, local: true) do |form| %>
  <% if tiipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tiipo.errors.count, "error") %> prohibited this tiipo from being saved:</h2>

      <ul>
      <% tiipo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :tiipo_nombre %>
  </div>

  <div class="field">
    <%= form.label :codigo %>
    <%= form.text_field :codigo, id: :tiipo_codigo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
